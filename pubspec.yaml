name: skarb_plugin
description: A new Flutter plugin project.
version: 3.3.0

environment:
  sdk: ">=2.19.6 <4.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.bitlica.skarb_plugin
        pluginClass: SkarbPlugin
      ios:
        pluginClass: SkarbPlugin
